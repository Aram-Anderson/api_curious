<h1>It's Like Uber, But For Github</h1>

<p>Logged in As: <%= current_user.username %></p>
<%= image_tag current_user.avatar_url, width: "150em" %>
<p>Public Repo Count: <%= current_user.public_repos_count %></p>
<p>Number of Followers: <%= current_user.followers_count %></p>
<p>Number of Users You're Following: <%= current_user.following_count %></p>
<p><%= link_to "Starred Repos", current_user.starred_url %></p>
<p>Commits by Week: <%= line_chart @commits.group_by_week(:week).count %></p>
<p>Your Repos:</p>
<% @repos.each do |repo| %>
<ul class="repos">
  <li><%= link_to repo.name, repo.url %></li>
  <ul>
  <li><p>Language: <%= repo.language %></p></li>
  <li><p>Created At: <%= time_ago_in_words(repo.created_at) %> ago</p></li>
  <li><p>Updated At: <%= time_ago_in_words(repo.updated_at) %> ago</p></li>
  </ul>
</ul>
<% end %>
